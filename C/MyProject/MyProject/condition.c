	/*
#include <stdio.h>

int main(void)
{
	int age;
	printf("몇 살이세요?");
	scanf_s("%d", &age);
		
	if (age >= 20)
		printf("성인입니다.");
	else if (age >= 8 && age < 20)
		printf("학생입니다.");
	else
		printf("어린이입니다.");


	for (int i = 1; i <= 15; i++)
	{
		if (i >= 6 && i<= 10)
		{
			if (i == 7) {
				printf("%d 번 학생은 결석입니다.\n", i);
				continue; // 조건에 맞다면 이후의 코드를 실행하지않고 바로 시작부분으로 반복
			}
			printf("%d번 학생은 조별발표 준비를 하세요\n", i); //continue 한다면 실행안됨
		}
	}
	 
	return 0;
}
	*/