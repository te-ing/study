export default function HomePage({ $target }) {
  const $page = document.createElement('div')

  this.state = []

  this.render = () => {
    $target.appendChild($page)
  }

  const init = () => {
    
  }

}